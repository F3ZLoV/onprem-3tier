---
- name: Install node_exporter on all nodes
  hosts: all
  become: true
  roles:
    - node-exporter

- name: Install monitoring stack on monitoring node
  hosts: monitoring
  become: true
  roles:
    - alertmanager
    - prometheus
    - grafana
